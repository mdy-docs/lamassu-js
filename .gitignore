build/

# Node / npm
node_modules/
*.log
.DS_Store

# Emscripten + Vite + VitePress build artifacts (rebuilt by CI / `make pkg` /
# `vite build` / `vitepress build`). Source IS committed.
packages/lamassu-js/dist/
web/dist/
docs-site/dist/
docs-site/.vitepress/cache/

# legacy playground artifacts (now built by Vite)
web/jsvm.js
web/jsvm.wasm

# npm pack output
*.tgz

# Claude Code local settings
.claude/
